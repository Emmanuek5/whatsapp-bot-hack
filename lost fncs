const now = new Date();
    const currentHour = now.getHours();

    if (currentHour >= 18 || currentHour < 9) {
      const closedMessage =
        "We are currently closed. Our working hours are from 9 AM to 6 PM.";
    msg.reply( closedMessage);
      return;
    }